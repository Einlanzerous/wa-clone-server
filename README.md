# NiQi Chat Server Code
The server code is fairly simple at the moment, watching for specific events around `send-message` and `connection`.

# To Start
After normal things, like `npm i`, with the expectation that you have typescript installed, run `npm run devStart` which will start `nodemon` against the server.
